name: Build arabella aosp Kernel

on:
  workflow_dispatch:
    inputs:
      include_ksu:
        description: 'Include KernelSU?'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout source
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y build-essential git curl wget bison flex zip bc cpio libssl-dev ccache python-is-python3 tar

    - name: Set short commit SHA
      id: vars
      run: echo "SHORT_SHA=$(echo ${GITHUB_SHA} | cut -c1-8)" >> $GITHUB_ENV

    - name: Cache ccache
      uses: actions/cache@v4
      with:
        path: ~/.cache/ccache_mikernel
        # Removed the 'device' input reference since your device is fixed (LMV600)
        key: ${{ runner.os }}-ccache-v60-${{ github.event.inputs.include_ksu }}-${{ env.SHORT_SHA }}
        restore-keys: |
          ${{ runner.os }}-ccache-v60-${{ github.event.inputs.include_ksu }}-
          ${{ runner.os }}-ccache-v60-

    - name: Setup zyc-clang toolchain
      run: |
        mkdir -p $HOME/zyc-clang
        cd $HOME/zyc-clang
        # Using Zyc-Clang 15 (Standard for many Snapdragon 865 kernels)
        wget https://github.com/ZyCromerZ/Clang/releases/download/15.0.7-20251111-release/Clang-15.0.7-20251111.tar.gz
        tar -zxvf Clang-15.0.7-20251111.tar.gz
        cd $GITHUB_WORKSPACE

    - name: Build kernel
      run: |
        # Exporting path for the toolchain
        export PATH="$HOME/zyc-clang/bin:$PATH"
        if [ "${{ github.event.inputs.include_ksu }}" = "true" ]; then
          # Ensure your build.sh is set up to handle the 'ksu' argument
          bash kernel.sh ksu
        else
          bash kernel.sh
        fi

    - name: Show ccache stats
      run: ccache -s

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        # Dynamic naming based on whether KSU is included
        name: LMV600${{ github.event.inputs.include_ksu == 'true' && '-KSU-SuSFS' || '-SuSFS' }}-Kernel
        path: |
          *.zip
          out/arch/arm64/boot/Image*
          out/arch/arm64/boot/dtb*
          out/arch/arm64/boot/dts/vendor/*.dtb
          
